# Case Lab — Real Web Project
Public site + functional Case dashboard + PWA install foundation + D1 schema.
The dashboard currently persists edits in localStorage for a fully working preview.
For production multi-user CMS, bind a dedicated Cloudflare D1 database and connect public/_worker.js API endpoints to it.
